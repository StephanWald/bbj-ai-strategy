# BBJ RAG -- Environment Variables
#
# Copy this file to .env before running:
#   cp .env.example .env
#
# Ollama must be running on the host with OLLAMA_HOST=0.0.0.0:11434
# so Docker containers can reach it via host.docker.internal.
#
# External ports are mapped above 10000 to avoid conflicts with common
# services (e.g., 10800 for the app, 10432 for Postgres).

# ---------- Database credentials ----------
# Used by the app (BBJ_RAG_ prefix maps to pydantic-settings fields)
BBJ_RAG_DB_HOST=localhost
BBJ_RAG_DB_PORT=5432
BBJ_RAG_DB_USER=bbj
BBJ_RAG_DB_PASSWORD=changeme
BBJ_RAG_DB_NAME=bbj_rag

# Used by the pgvector container directly (Postgres image env vars)
POSTGRES_USER=bbj
POSTGRES_PASSWORD=changeme
POSTGRES_DB=bbj_rag

# ---------- Ollama (host machine) ----------
OLLAMA_HOST=http://host.docker.internal:11434

# ---------- Embedding configuration ----------
BBJ_RAG_EMBEDDING_MODEL=qwen3-embedding:0.6b
BBJ_RAG_EMBEDDING_DIMENSIONS=1024
BBJ_RAG_EMBEDDING_PROVIDER=ollama

# ---------- Chat (Claude API) ----------
# Required for /chat -- get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
# Optional overrides (defaults shown):
# BBJ_RAG_CHAT_MODEL=claude-sonnet-4-5-20250929
# BBJ_RAG_CHAT_MAX_TOKENS=2048

# ---------- Application ----------
ENV=production
APP_PORT_EXTERNAL=10800
DB_PORT_EXTERNAL=10432

# ---------- Source data path (host machine) ----------
# Path to the directory containing ingestion source data.
# Mounted read-only into the app container at /data.
INGESTION_DATA_PATH=./data
