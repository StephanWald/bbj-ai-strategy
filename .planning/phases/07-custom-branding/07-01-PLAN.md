---
phase: 07-custom-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/img/dwc-logo.png
  - static/img/favicon.png
  - static/img/logo.svg
  - docusaurus.config.ts
autonomous: true

must_haves:
  truths:
    - "Browser tab shows a BASIS-branded favicon (blue DWC icon), not the Docusaurus dinosaur"
    - "Navbar displays the DWC logo image to the left of the 'BBj AI Strategy' title text"
    - "The default Docusaurus dinosaur logo.svg has been removed from static/img/"
  artifacts:
    - path: "static/img/dwc-logo.png"
      provides: "Navbar logo image (198x199px, dark background with white DWC icon)"
      min_lines: 0
    - path: "static/img/favicon.png"
      provides: "PNG favicon (32x32) for modern browsers"
      min_lines: 0
    - path: "docusaurus.config.ts"
      provides: "Favicon path and navbar logo configuration"
      contains: "favicon: 'img/favicon.png'"
  key_links:
    - from: "docusaurus.config.ts"
      to: "static/img/favicon.png"
      via: "favicon config property"
      pattern: "favicon:\\s*['\"]img/favicon\\.png['\"]"
    - from: "docusaurus.config.ts"
      to: "static/img/dwc-logo.png"
      via: "navbar.logo.src config property"
      pattern: "src:\\s*['\"]img/dwc-logo\\.png['\"]"
---

<objective>
Add BASIS International brand assets to the site — favicon and navbar logo — and remove the Docusaurus default dinosaur.

Purpose: Replace default Docusaurus branding with BASIS identity so the site looks like an official BASIS publication, consistent with the bbj-dwc-tutorial reference site.
Output: `dwc-logo.png` and `favicon.png` in `static/img/`, updated `docusaurus.config.ts` with favicon path and navbar logo, `logo.svg` deleted.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.1-ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-custom-branding/07-RESEARCH.md
@docusaurus.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy brand assets and remove default logo</name>
  <files>static/img/dwc-logo.png, static/img/favicon.png, static/img/logo.svg</files>
  <action>
Copy two image files from the reference project into this project's `static/img/` directory:

```bash
cp /Users/beff/_workspace/bbj-dwc-tutorial/static/img/dwc-logo.png /Users/beff/_workspace/bbj-ai-strategy/static/img/dwc-logo.png
cp /Users/beff/_workspace/bbj-dwc-tutorial/static/img/favicon.png /Users/beff/_workspace/bbj-ai-strategy/static/img/favicon.png
```

Then delete the default Docusaurus dinosaur logo:

```bash
rm /Users/beff/_workspace/bbj-ai-strategy/static/img/logo.svg
```

Keep `favicon.ico` — it's already identical to the reference project and serves as a fallback for older browsers.

After copying, verify the files exist and have expected sizes:
- `dwc-logo.png` should be ~12KB (198x199px navbar logo)
- `favicon.png` should be ~2KB (32x32px favicon)
- `logo.svg` should no longer exist
- `favicon.ico` should still exist (~3.6KB)
  </action>
  <verify>
```bash
ls -la static/img/
# Expected: dwc-logo.png (~12KB), favicon.png (~2KB), favicon.ico (~3.6KB)
# Expected: NO logo.svg
```
  </verify>
  <done>static/img/ contains dwc-logo.png, favicon.png, and favicon.ico. logo.svg is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update docusaurus.config.ts with favicon path and navbar logo</name>
  <files>docusaurus.config.ts</files>
  <action>
Make two changes to `docusaurus.config.ts`:

1. **Change favicon path** from `'img/favicon.ico'` to `'img/favicon.png'` (line 8):
```typescript
favicon: 'img/favicon.png',
```

2. **Add navbar logo** — add a `logo` object to `themeConfig.navbar`, before the existing `items` array (around line 72):
```typescript
navbar: {
  title: 'BBj AI Strategy',
  logo: {
    alt: 'BASIS Logo',
    src: 'img/dwc-logo.png',
  },
  items: [
```

The logo has a dark rounded-square background with a white icon — it renders well on both light and dark mode backgrounds. No `srcDark` variant is needed.

Do NOT change any other config properties (prism themes, mermaid, footer, etc.).
  </action>
  <verify>
```bash
# Verify favicon config
grep "favicon:" docusaurus.config.ts
# Expected: favicon: 'img/favicon.png',

# Verify logo config
grep -A3 "logo:" docusaurus.config.ts
# Expected: logo: { alt: 'BASIS Logo', src: 'img/dwc-logo.png' }

# Build test
npm run build
# Expected: Build succeeds with no errors
```
  </verify>
  <done>docusaurus.config.ts references favicon.png and displays dwc-logo.png in the navbar. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `ls static/img/` shows: `dwc-logo.png`, `favicon.ico`, `favicon.png` — no `logo.svg`
2. `grep "favicon:" docusaurus.config.ts` returns `favicon: 'img/favicon.png'`
3. `grep -A2 "logo:" docusaurus.config.ts` shows the logo object with `src: 'img/dwc-logo.png'`
4. `npm run build` completes without errors
</verification>

<success_criteria>
- Browser tab shows BASIS-branded favicon (not Docusaurus dinosaur)
- Navbar displays DWC logo image alongside "BBj AI Strategy" title
- No references to logo.svg remain in config files
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-custom-branding/07-01-SUMMARY.md`
</output>
