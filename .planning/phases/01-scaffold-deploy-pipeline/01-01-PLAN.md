---
phase: 01-scaffold-deploy-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docusaurus.config.ts
  - sidebars.ts
  - package.json
  - tsconfig.json
  - src/pages/index.tsx
  - src/pages/index.module.css
  - src/css/custom.css
  - docs/01-bbj-challenge/_category_.json
  - docs/01-bbj-challenge/index.md
  - docs/02-strategic-architecture/_category_.json
  - docs/02-strategic-architecture/index.md
  - docs/03-fine-tuning/_category_.json
  - docs/03-fine-tuning/index.md
  - docs/04-ide-integration/_category_.json
  - docs/04-ide-integration/index.md
  - docs/05-documentation-chat/_category_.json
  - docs/05-documentation-chat/index.md
  - docs/06-rag-database/_category_.json
  - docs/06-rag-database/index.md
  - docs/07-implementation-roadmap/_category_.json
  - docs/07-implementation-roadmap/index.md
  - static/.nojekyll
autonomous: true

must_haves:
  truths:
    - "`npm run build` completes with zero errors"
    - "All 7 chapter folders appear in the sidebar in correct order (1-7)"
    - "Each chapter heading in the sidebar is clickable and loads a placeholder page with teaser content"
    - "The landing page shows the BBj AI Strategy title and tagline, not the Docusaurus default dinosaur page"
    - "`npm run start` launches a dev server and the site renders correctly at localhost"
  artifacts:
    - path: "docusaurus.config.ts"
      provides: "Site metadata, baseUrl, trailingSlash, @docusaurus/faster config"
      contains: "baseUrl: '/bbj-ai-strategy/'"
    - path: "sidebars.ts"
      provides: "Autogenerated sidebar configuration"
      contains: "strategySidebar"
    - path: "docs/01-bbj-challenge/_category_.json"
      provides: "Chapter 1 sidebar config"
      contains: "position.*1"
    - path: "docs/07-implementation-roadmap/_category_.json"
      provides: "Chapter 7 sidebar config"
      contains: "position.*7"
    - path: "docs/01-bbj-challenge/index.md"
      provides: "Chapter 1 placeholder with teaser"
    - path: "src/pages/index.tsx"
      provides: "Custom landing page"
      contains: "BBj AI Strategy"
    - path: "package.json"
      provides: "@docusaurus/faster dependency"
      contains: "@docusaurus/faster"
  key_links:
    - from: "docusaurus.config.ts"
      to: "sidebars.ts"
      via: "navbar sidebarId references sidebar key name"
      pattern: "sidebarId.*strategySidebar"
    - from: "sidebars.ts"
      to: "docs/"
      via: "autogenerated dirName reads folder structure"
      pattern: "type.*autogenerated"
    - from: "docs/*/_category_.json"
      to: "docs/*/index.md"
      via: "link.type doc with id pointing to index"
      pattern: "link.*type.*doc"
---

<objective>
Initialize a Docusaurus project with Rspack-accelerated builds, configure it for GitHub Pages deployment, and create the 7-chapter folder structure with placeholder content.

Purpose: This is the foundation for the entire site. Every subsequent phase depends on a buildable Docusaurus project with the correct chapter structure and GitHub Pages configuration.

Output: A complete Docusaurus project at the repo root that builds successfully, with all 7 chapters visible in the sidebar and a customized landing page.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-deploy-pipeline/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Docusaurus and configure for GitHub Pages with Rspack</name>
  <files>
    docusaurus.config.ts
    sidebars.ts
    package.json
    tsconfig.json
    src/css/custom.css
    static/.nojekyll
  </files>
  <action>
The working directory `/Users/beff/_workspace/bbj-ai-strategy/` already has `.git/`, `.planning/`, `.claude/`, and `bbj-llm-strategy.md`. The Docusaurus scaffold must coexist with these.

**Step 1: Scaffold into temp directory and move files to root.**

```bash
cd /Users/beff/_workspace
npx create-docusaurus@latest bbj-ai-strategy-temp classic --typescript
```

Then move the scaffold files into the existing `bbj-ai-strategy/` directory:

```bash
# Move all scaffold contents (except .git which doesn't exist in temp) into existing repo
cp -r bbj-ai-strategy-temp/* bbj-ai-strategy/
cp -r bbj-ai-strategy-temp/.* bbj-ai-strategy/ 2>/dev/null || true
rm -rf bbj-ai-strategy-temp
```

**Step 2: Install @docusaurus/faster.**

```bash
cd /Users/beff/_workspace/bbj-ai-strategy
npm install @docusaurus/faster
```

**Step 3: Replace `docusaurus.config.ts` with the project-specific config.**

Use the EXACT config from `01-RESEARCH.md` "Complete docusaurus.config.ts for This Project" section. Key settings:
- `title: 'BBj AI Strategy'`
- `tagline: 'Intelligent Code Assistance Across Four Generations of Business BASIC'`
- `url: 'https://beff.github.io'`
- `baseUrl: '/bbj-ai-strategy/'`
- `organizationName: 'beff'`
- `projectName: 'bbj-ai-strategy'`
- `trailingSlash: false`
- `future: { v4: true, experimental_faster: true }`
- `blog: false`
- Navbar with `sidebarId: 'strategySidebar'` and GitHub link
- Footer with copyright
- `colorMode: { respectPrefersColorScheme: true }`

**Step 4: Replace `sidebars.ts`.**

Use the exact config from research:
```typescript
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';
const sidebars: SidebarsConfig = {
  strategySidebar: [{type: 'autogenerated', dirName: '.'}],
};
export default sidebars;
```

**Step 5: Delete scaffold blog content.**

```bash
rm -rf blog/
```

**Step 6: Delete scaffold tutorial docs** (will be replaced in Task 2).

```bash
rm -rf docs/intro.md docs/tutorial-basics/ docs/tutorial-extras/
```

**Step 7: Verify `.nojekyll` exists in `static/`** (scaffold should include it).

CRITICAL: Do NOT use `src/components/HomepageFeatures/` from the scaffold — it will be replaced when we modify the landing page. It can be deleted or left for Task 2 to handle.
  </action>
  <verify>
    Run `npm run build` — must complete with zero errors. Check that `package.json` contains `@docusaurus/faster`. Check that `docusaurus.config.ts` contains `baseUrl: '/bbj-ai-strategy/'` and `experimental_faster: true`. Check that `sidebars.ts` contains `strategySidebar`. Check that `blog/` directory does not exist. Check that `static/.nojekyll` exists.
  </verify>
  <done>
    Docusaurus project exists at repo root. Config has correct GitHub Pages settings (baseUrl, trailingSlash, url). @docusaurus/faster is installed and enabled. Blog is disabled. Scaffold tutorial content is removed. Build succeeds (may have warnings about empty docs, which Task 2 fixes).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 7-chapter folder structure and customize landing page</name>
  <files>
    docs/01-bbj-challenge/_category_.json
    docs/01-bbj-challenge/index.md
    docs/02-strategic-architecture/_category_.json
    docs/02-strategic-architecture/index.md
    docs/03-fine-tuning/_category_.json
    docs/03-fine-tuning/index.md
    docs/04-ide-integration/_category_.json
    docs/04-ide-integration/index.md
    docs/05-documentation-chat/_category_.json
    docs/05-documentation-chat/index.md
    docs/06-rag-database/_category_.json
    docs/06-rag-database/index.md
    docs/07-implementation-roadmap/_category_.json
    docs/07-implementation-roadmap/index.md
    src/pages/index.tsx
    src/pages/index.module.css
  </files>
  <action>
**Step 1: Create 7 chapter folders with `_category_.json` and `index.md`.**

For each chapter, create a folder under `docs/` with a `_category_.json` and `index.md`.

**`_category_.json` pattern** (repeat for all 7, changing label, position, and id):

```json
{
  "label": "{Chapter Label}",
  "position": {N},
  "link": {
    "type": "doc",
    "id": "{folder-name}/index"
  }
}
```

**Chapter data table:**

| # | Folder Name | Label | Teaser |
|---|-------------|-------|--------|
| 1 | `01-bbj-challenge` | The BBj Challenge | BBj spans four generations of Business BASIC, from character terminals to modern web UIs. This chapter explains why generic LLMs consistently fail on BBj code and why off-the-shelf tools like GitHub Copilot cannot bridge the gap. |
| 2 | `02-strategic-architecture` | Strategic Architecture | Rather than building isolated point solutions, the BBj AI strategy centers on a unified infrastructure — a shared fine-tuned model and RAG pipeline that powers IDE completion, documentation chat, and future AI capabilities from a single source of truth. |
| 3 | `03-fine-tuning` | Fine-Tuning the Model | The foundation of BBj-aware AI is a fine-tuned language model trained on generation-labeled BBj code. This chapter covers training data structure, base model selection, the LoRA fine-tuning approach, and local hosting through Ollama. |
| 4 | `04-ide-integration` | IDE Integration | A VSCode extension brings BBj intelligence directly into the developer's editor. This chapter details the extension architecture, Langium-based language parsing, and completion mechanisms including ghost text and context-aware suggestions. |
| 5 | `05-documentation-chat` | Documentation Chat | An AI-powered chat system embedded in the BBj documentation site provides generation-aware answers that generic chat services cannot. This chapter covers the chat architecture, why off-the-shelf solutions fail for BBj, and how responses adapt to the user's target generation. |
| 6 | `06-rag-database` | RAG Database Design | The retrieval-augmented generation database organizes BBj documentation across four generations with rich metadata for precise retrieval. This chapter details the document structure, multi-generation metadata schema, and retrieval strategies that power both the IDE extension and documentation chat. |
| 7 | `07-implementation-roadmap` | Implementation Roadmap | Turning strategy into action requires phased delivery, realistic resource planning, and clear success criteria. This chapter presents the implementation phases, infrastructure requirements, risk assessment, and measurable metrics for tracking progress. |

**`index.md` pattern** for each chapter:

```markdown
---
sidebar_position: 1
title: "{Chapter Label}"
description: "{First sentence of teaser}"
---

# {Chapter Label}

{Full teaser paragraph - 2-3 sentences from table above}

:::note Coming Soon
This chapter is under active development. Content will be published incrementally as research and writing progress.
:::
```

**Step 2: Customize the landing page.**

Replace `src/pages/index.tsx` with a simple but professional landing page. Key elements:
- Hero section with site title ("BBj AI Strategy") and tagline
- A "Read the Strategy" button linking to `/docs/01-bbj-challenge`
- A brief description section (2-3 sentences about what this site covers)
- Chapter overview section: 7 cards/links showing each chapter title and a one-line summary

Delete or replace `src/components/HomepageFeatures/` — the scaffold's default feature component references dinosaurs and is not relevant. Either remove the directory entirely or replace the content.

Replace `src/pages/index.module.css` (or create it if the scaffold uses `styles.module.css`) with minimal styling for the landing page layout.

Keep the landing page simple — Docusaurus defaults for color scheme. No custom images or logos. The page should look clean and professional with just text and links.

**Step 3: Verify the complete build.**

```bash
npm run build
```

Must succeed with zero errors. All 7 chapters must appear in the generated sidebar.
  </action>
  <verify>
    Run `npm run build` — zero errors. Verify all 7 `_category_.json` files exist with correct positions (1-7). Verify all 7 `index.md` files exist with frontmatter. Run `npm run serve` and visually confirm: (1) landing page shows "BBj AI Strategy" title, (2) sidebar shows all 7 chapters in order, (3) clicking each chapter loads its placeholder page. Check that `src/components/HomepageFeatures/` is either removed or updated (no dinosaur references).
  </verify>
  <done>
    All 7 chapter folders exist with correct `_category_.json` (labels without numeric prefix, positions 1-7, links to index.md). Each chapter has an `index.md` with teaser content and "Coming Soon" note. Landing page displays BBj AI Strategy branding with chapter navigation. `npm run build` produces zero errors. The sidebar renders all 7 chapters in correct order.
  </done>
</task>

</tasks>

<verification>
Run the following checks to verify the plan is complete:

1. `npm run build` exits with code 0 (zero errors)
2. `ls docs/0*/_category_.json | wc -l` returns 7
3. `ls docs/0*/index.md | wc -l` returns 7
4. `grep -c 'strategySidebar' sidebars.ts` returns at least 1
5. `grep -c 'baseUrl.*bbj-ai-strategy' docusaurus.config.ts` returns at least 1
6. `grep -c 'experimental_faster.*true' docusaurus.config.ts` returns at least 1
7. `grep -c '@docusaurus/faster' package.json` returns at least 1
8. `test -f static/.nojekyll && echo "OK"` returns OK
9. `test ! -d blog && echo "OK"` returns OK (blog deleted)
10. `npm run serve` — landing page renders with "BBj AI Strategy", sidebar shows 7 chapters in order
</verification>

<success_criteria>
- Docusaurus project builds successfully with `npm run build` (zero errors)
- @docusaurus/faster is installed and enabled (experimental_faster: true)
- baseUrl is `/bbj-ai-strategy/` and trailingSlash is false
- All 7 chapters appear in sidebar with clean labels (no numeric prefixes) in correct order
- Each chapter has a clickable placeholder page with teaser content
- Landing page shows BBj AI Strategy branding, not Docusaurus defaults
- Blog is disabled and removed
- static/.nojekyll exists for GitHub Pages compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-deploy-pipeline/01-01-SUMMARY.md`
</output>
